# 服务器位置
SERVER_IP=10.42.0.1
SERVER_PORT=8080

# 客户端并发数（同一台树莓派上同时起几个client）
NUM_CLIENTS=14

# 算法与数据
SKETCH=asketch
DATA_PATH=/home/sketch/DS/Data_Sketches/database/test_zipf.csv
FIELD_NAME=item_id
FIELD_TYPE=int

# 负载参数
MERGE_EVERY=1000
STREAM_RATE=10

# 可执行与日志路径
BIN_PATH=/home/sketch/DS/Data_Sketches/app/sketcher
LOG_DIR=/home/sketch/DS/logs

# 启动错峰（秒），避免同一时刻突发
START_STAGGER_SECS=1